import { XummPkce } from "../src/index";

describe(`Tests: TODO`, () => {
  const auth = new XummPkce();

  describe("TODO", () => {
    it(`'TODO`, async () => {
      expect(1).toEqual(1);
    });
  });
});
